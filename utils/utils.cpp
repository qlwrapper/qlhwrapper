#include <utils/utilities.h>

using namespace QuantLib;

namespace utils {

    void test() {
        //auto s = TextFileReader<char>::readAsString(std::string("temp.txt"));
        //auto ret1 = TextFileReader<char>::readLineByLine(std::string("temp.txt"));
        //auto ret2 = TextFileReader<char>::readLineByLine<ParseDelimitedLineProcessor<char, 9>>(std::string("temp.txt"));
    }
};